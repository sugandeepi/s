#include<stdio.h>
int main()
{
    char a[50],b[50];
    printf("enter the two strings:");
    scanf("%s %s",a,b);
    if(a>b)
    {
        printf("%s",a);
        
    }
else
{
    printf("%s",b);
}
}
