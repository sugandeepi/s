#include<stdio.h>
int array[20],size,i,largest;
{
printf("enter the size of an array");
scanf("%d",&size);
printf("enter the elements %d in an array");
for(i=0;i<size;i++)
scanf("%d",&array[i]);
largest=array[0];
for("i=1;i<size;i++)
{
if(largest<array[i]);
largest=array[i]);
}
printf("the largest element in an array is %d",largest);
return o;
}


